<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ceu.marten.bplux"
    android:versionCode="1"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ceu.marten.activities.WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ceu.marten.activities.SettingsActivity"
            android:label="@string/settingsTitle"
            android:parentActivityName="ceu.marten.activities.WelcomeActivity" >
        </activity>
        <activity
            android:name="ceu.marten.activities.GraphActivity"
            android:label="@string/graphtitle"
            android:parentActivityName="ceu.marten.activities.WelcomeActivity" >
        </activity>
       
    </application>

</manifest>
